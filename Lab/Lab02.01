#include <stdio.h>

int main() {
    int N , i = 0 ; 
    if(scanf( "%d" , &N ) != 1) {
        printf("%d",N);
        while( getchar() != '\n' ) ; 
        return 1 ; 
    }
    
    while ( i < N ) {
        printf("Hello Loop!\n") ;
        i++ ;
    }

    return 0 ;
}
