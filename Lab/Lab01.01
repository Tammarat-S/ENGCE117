#include <stdio.h>

int main() {
    int N = 0 , i ; 
    if(scanf( "%d" , &N ) != 1) {
        printf("%d",N); 
        return 1 ; 
    }
    for( i = 0 ; i < N ; i++ ) {
        printf( "Hello Loop!\n" ) ;
    }
    return 0 ;
}
