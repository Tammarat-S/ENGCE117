#include <stdio.h>
#include <string.h>

void reverse( char *str1 , char *str2 ) ;

int main () {
    char text[50] = "I Love You" ;
    char out[50] ;
    reverse( text , out ) ;
    printf( "%s" , out) ;
    return 0 ;
}

void reverse( char *str1 , char *str2 ) {
    int S ;
    S = strlen(str1) ;
    for( int i = 0 ; i < S ; i ++ ) {
        str2[i] = str1[S-1-i] ;
    }
    str2[S] = '\0' ;
}
